# ContentGuard for macOS

This directory holds the macOS side of ContentGuard: the same kind of
self-imposed, hard-to-disable content blocking the Android app
(`app/`) implements, extended to a Mac via fleet management, a native
on-device blocker, and app execution control.

## Build order

The macOS build is six phases, done strictly in order - each one depends on
state the previous phase left behind, and each has its own verification
checklist before moving on:

| Phase | What | Status |
|---|---|---|
| 0 | First-boot Mac setup: accounts, FileVault, Find My Mac, dev tools, WARP | done - accounts, Find My Mac, dev tools, Zero Trust/WARP all verified. FileVault and the admin account rename deliberately deferred; must land before Phase 5. |
| 1 | Fleet MDM on Fly.io, enrollment, `.mobileconfig` profiles | **done** - Fleet deployed, Mac enrolled and confirmed supervised, all 4 profiles pushed and verified (the 4th being Phase 2's PPPC profile, `profiles/pppc.mobileconfig` - pushed later than the other 3, once Phase 2 needed it), Fleet Premium purchased, Recovery Lock set and reboot-tested, all six verification tests passed, WARP switch locked, Gateway NSFW category block and DoH-provider IP blocklist both configured. FileVault, the admin account rename, and rotating the tunnel connector token are deliberately deferred to Phase 5's pre-lockdown checklist. **Repo-hygiene bug found and fixed while starting Phase 3**: the real, filled-in PPPC content from the commit above landed in a stray `mac/profiles/pppc.mobileconfig` instead of updating the actual `profiles/pppc.mobileconfig` at the repo root - every other reference in this repo (`build-pkg.sh`, `PHASE_2_SIGNING_TEST.md`, this file) points at the root-level path, which had silently been left with its original `__BUNDLE_ID__`/`__CODE_REQUIREMENT__` placeholders the whole time. Fleet itself was fine (the real content was pushed there directly, hence "confirmed working end-to-end on the real Mac" above), but the repo was internally inconsistent with what's actually deployed. Fixed: moved the real content into `profiles/pppc.mobileconfig`, deleted the stray `mac/profiles/` directory. |
| 2 | Native AI blocker: `ContentGuardAgent` (capture + NudeNet) + `ContentGuardDaemon` (tamper anchor) | **done** - real end-to-end detection **confirmed working live on the Mac**: real screen capture -> ONNX Runtime + CoreML inference on the real model -> correct confidence-threshold gating (a 0.0001-confidence blip correctly ignored, a genuine 0.83-confidence detection correctly triggered) -> `com.google.Chrome` actually force-terminated. The core of Phase 2 is proven end-to-end, not just built. Daemon also run live for the first time: correctly detected a missing agent heartbeat while a risky app was running and tried to fail closed, but `FallbackCover`'s lock mechanism was broken - it assumed `CGSession -suspend`, which Apple removed all the way back in Big Sur (2020) and was never actually run until now. Replaced with `pmset displaysleepnow`, and **confirmed working end-to-end on the real Mac**: display genuinely went to sleep and required the real login password to wake. "Require password immediately after sleep" in System Settings -> Lock Screen was already enabled, no setup gap after all. **Deliberate reaction change from the original spec**: on detection, the agent force-quits the frontmost app instead of covering the screen - no blackout, no daemon notification for this path. Chosen for disguise (a "crashed app" is much less suspicious/tempting-to-circumvent than an obvious block screen) over the cooldown protection the blackout approach gave; known and accepted tradeoff is that nothing stops immediately reopening the same app/site right after - no forced wait, no escalation on repeated triggers. The daemon's separate tamper-resistance (heartbeat monitoring, fail-closed if the agent goes quiet, escalation on repeated agent kills) is unchanged and still fully active - see `ContentGuardAgent/Sources/main.swift`'s `quitFrontmostApp()`. Also fixed a real SIGTRAP crash this surfaced: AppKit calls (`NSPanel`, `NSWorkspace`) were happening off the main thread from FrameProcessor's background queue - `OverlayManager.cover()/clear()` and `quitFrontmostApp()` both dispatch to main internally now. **Real launchd-managed install test completed, and it surfaced a genuine incident**: the agent's startup-failure paths still called `overlayManager.cover()` directly (a leftover from before the quit-frontmost-app reaction change above), and with `KeepAlive=true` plus a *persistent* startup failure (missing Screen Recording permission for the freshly-installed `/usr/local/bin/` path), that produced a real, confirmed lockout - a black screen surviving every restart, recovered live via ⌘+Space -> blind-typed Terminal -> `launchctl bootout` (the overlay's `.nonactivatingPanel` style mask doesn't steal keyboard focus, which is what made blind recovery possible at all). Fixed by removing both `overlayManager.cover()` calls from the agent's startup-failure paths entirely - the daemon's own heartbeat-based fail-closed (already proven escapable via `pmset displaysleepnow` + real password) is the backstop for that case now, not a second local cover mechanism with no matching `.clear()`. Once that was fixed, launchd-managed capture still failed with `SCStreamErrorDomain -3801` ("declined TCCs") on every `launchctl bootstrap`-triggered launch, while the identical signed binary worked fine launched directly from Terminal - chased at length (code signature match confirmed via `codesign` against the exact stored TCC requirement, `killall tccd`, toggling the Settings switch off/on, `tccutil reset`, a from-scratch re-sign with a fresh inode - none of it fixed the launchd-specific case) before landing on the real cause: this repo never actually had a PPPC profile pushed for Phase 2 (see Phase 1's row above), so every prior Screen Recording grant went through an ad-hoc interactive path (Settings toggle, a foreground Terminal launch) that doesn't reliably extend to a headless `launchd`-started background agent. Wrote and pushed `profiles/pppc.mobileconfig` (`Authorization: Allow`, not the signing test's `AllowStandardUserToSetSystemService` - that flag needs a live interactive click, which a headless LaunchAgent can never provide) and **confirmed working end-to-end on the real Mac**: repeated `launchctl bootout`/`bootstrap` cycles now capture and classify cleanly every time, `launchctl print` showing `state = running` with a live pid. Ran the real launchd verification checklist against that working install: **agent relaunch after kill confirmed** (`kill -9` -> `KeepAlive` brings a fresh pid back within seconds, no re-grant needed). **Fail-closed on agent trouble confirmed** (a kill-loop drove the agent through several rapid relaunches; the daemon genuinely locked the screen via `FallbackCover` - though it was `HeartbeatMonitor`'s heartbeat-grace check that won the race, not `EscalationManager`'s 5-kills/60s counter specifically, since `launchd`'s own relaunch throttling introduced a heartbeat gap first; the specific escalation-counter path is still unconfirmed in isolation). **Sleep/wake resume confirmed** (~36s real sleep, `CaptureManager.handleWake()`'s silent stream rebuild worked correctly, capture resumed with no manual relaunch). **Full-screen coverage surfaced a real, separate bug, now fixed**: testing detection during real macOS full-screen mode produced a string of near-zero-confidence classifications on content confirmed genuinely explicit, immediately followed by the `SCStream` silently dying entirely - capture stopped producing frames, but `HeartbeatClient.captureActive` (set once when `start()` succeeds, never re-evaluated - see that file) kept reporting healthy on every heartbeat, so the daemon's existing grace-window check never noticed: heartbeats were arriving exactly on schedule, they just contained a frozen frame count for 5+ minutes. Fixed the detection gap: `HeartbeatMonitor` now separately tracks `framesProcessed` staleness (`ContentGuardConfig.frameStallGraceSeconds`, 30s) alongside heartbeat-arrival staleness, and fails closed on either signal - a stalled-but-still-heartbeating agent now gets caught, and no longer incorrectly auto-clears an active fallback cover either (that logic used to trust any heartbeat arriving at all as proof of live capture). Isolated the root cause too: the same content reloaded in a normal windowed browser scored realistically (0.28, then a genuine 0.85-confidence detection that correctly force-terminated Chrome) rather than the near-zero scores seen in full-screen - confirming this was never a hard case for the classifier, full-screen mode itself was degrading what the classifier saw. Re-tested full-screen afterward and it worked cleanly the second time (real detection, real block, `SCStream` stayed healthy throughout) - so the original failure looks like a transient/non-deterministic `SCStream` hiccup rather than a reliable full-screen-specific break, which is exactly the kind of failure the new frame-stall detection exists to catch when it recurs, rather than something requiring a capture-side fix right now. **All four items of the real launchd verification checklist now pass**: relaunch after kill, fail-closed on agent trouble, sleep/wake resume, full-screen coverage. **`EscalationManager.classifyExit(pid:)` implemented for real and confirmed working on the real Mac**: it had been a permanent stub returning `.unknown` for every exit, since the daemon doesn't launch the agent (launchd does, per `LaunchAgents/com.contentguard.agent.plist`) so there's no parent-process `wait()` to observe a real exit status/signal through - fixed with a raw Darwin `kqueue`/`kevent()` watcher (`EVFILT_PROC`, `NOTE_EXIT`/`NOTE_EXITSTATUS`), the only way a non-parent-but-privileged process can get a PID's real wait-status on Darwin, decoding the POSIX wait-status bit layout by hand since the `WIFEXITED`/`WEXITSTATUS`/etc. C macros don't bridge into Swift. Verified live with two real signals sent to the running agent: `kill -9` correctly logged `agent exit observed: terminated(signal: 9)` (and counts toward the 5-kills/60s escalation threshold); `kill -SIGSEGV` correctly logged `agent exit observed: crash(signal: 11)` and did **not** trigger escalation - confirming the crash-vs-tampering distinction this whole file exists for actually holds up against a real signal, not just the design intent. Only remaining loose end from this stretch of testing: the frame-stall detection (`frameStallGraceSeconds`) hasn't yet been observed actually firing live (the full-screen retest that would have exercised it happened to stay healthy) - worth a deliberate test if a natural recurrence doesn't show up first. **A real `sudo reboot` test (not just launchd bootout/bootstrap cycles) surfaced a genuine boot-time race, now fixed**: right after login, the daemon locked the screen almost immediately, forcing a second login. Root cause: `HeartbeatMonitor`'s grace-window check has always treated "never received a heartbeat this daemon instance" as overdue immediately (deliberately, to close a real gap where a daemon restart mid-session with a risky app already open shouldn't get a free pass) - but at a real cold boot the daemon comes up well before login, and the instant a user logs in, macOS relaunches login items while `RunningAppCheck.isRiskyAppRunning()` counts any ordinary `/Applications` app (not just Chrome) as risky and the agent's ScreenCaptureKit stream is still spinning up - so the very next grace-check tick saw "no heartbeat yet" + "a login item is running" and failed closed within seconds of typing the password. Fixed with a one-time, bounded startup grace period (`ContentGuardConfig.agentStartupGraceSeconds`, 45s) timed from daemon start rather than from the last heartbeat - once elapsed, the original immediate-overdue behavior applies for the rest of that daemon instance's life, so this doesn't reopen an ongoing gap, just delays when the check starts being strict. Rebuild/redeploy since this fix has happened (see below), but the reboot itself hasn't been retested yet - worth doing once the app-lock verification below settles. **Added a new per-app lockout capability, closing the quit-frontmost-app tradeoff called out above**: `AppLockManager` (daemon-side, new) tracks how many times each app has been force-quit for a real detection, and once an app hits `appBlockCountThreshold` (4) it's locked out from relaunching for `appLockDurationSeconds` (10 minutes) - enforced by the daemon itself, polling the process table every 2s and killing any matching process directly (root, no dependency on the agent staying alive, same "only the daemon itself can end it" tamper-resistance philosophy as `BlackoutTimer`). The agent reports each detection-triggered quit over a new `appDetection` IPC message; `AppLockManager` independently refuses to ever track a `neverTerminateBundleIDs` entry regardless of what a message claims, as defense against the socket's existing user-writable trust model. **Deploying this surfaced a real process gap, not a code bug**: the Mac had silently been building from a commit several rounds old the entire time (a local, uncommitted Xcode-driven `project.pbxproj` edit was blocking every `git pull` from progressing, and `xcodebuild` doesn't care whether a pull succeeded - it just compiles whatever's on disk), so an initial "4 real detections, still no lockout" test result was against code that didn't contain the feature at all, not a confirmed bug. Once resolved (discard the stale local pbxproj edit, since the repo's own commit already carried an equivalent fix, then pull clean), the *first* genuine fresh build surfaced two real, previously-undiscovered issues of its own: a `heartbeatClient` reference inside `quitFrontmostApp()`'s closure needing explicit `self.` (this project sets `default-isolation=MainActor`, which changes closure-capture requirements the moment an instance member is actually referenced - fixed), and a stale `SWIFT_OBJC_BRIDGING_HEADER` build setting on the daemon target pointing at a bridging header file that has never existed anywhere in the repo (pure-Swift target, no Objective-C interop needed - setting removed). Also caught and fixed a real bug in the agent's own detection-report code along the way: `quitFrontmostApp()` was reading `NSRunningApplication.executableURL` *after* calling `forceTerminate()` on the same object, which silently produced no report at all - fixed by capturing it before termination, matching how `bundleID` was already safely captured. **Confirmed working end-to-end on the real Mac after all of the above**: a real 4-in-a-row detection test produced exactly `app detection recorded: com.google.Chrome (1/4)` through `(4/4)`, `com.google.Chrome hit 4 detections - locking it out for 600s`, and a relaunch attempt killed within 3 seconds (`tried to relaunch while locked out - killing pid ...`). **A battery-drain report ("has guard been running smoothly?") surfaced a real energy problem, now fixed**: Activity Monitor showed `ContentGuardAgent` as the single highest Energy Impact process running (126.8, above Chrome, above everything) during completely ordinary use. Root cause, confirmed via the agent's own debug log: the skin-tone prefilter's threshold (0.05) barely cleared normal desktop "skin tone noise" (~0.066-0.069) - 36 of the last 42 captured frames were reaching a full ONNX Runtime + CoreML inference continuously, the "load shedder" shedding almost nothing. Raised to 0.15, with real margin confirmed on both sides by this session's own data (normal-use noise floor ~0.05-0.09, every real detection this session needed 0.2+ before the classifier responded at all) - **confirmed fixed**: Energy Impact dropped to 0.0 after redeploying, indistinguishable from any other idle background app. Also removed the per-frame `[debug]` logging in `FrameProcessor.swift`, unconditional on every 3s tick and explicitly flagged in its own comment as temporary - it had served its purpose confirming the pipeline end-to-end, extensively, across this whole session. **That same battery investigation surfaced a second, more serious problem**: the daemon's frame-stall detection (added earlier) fired correctly live for the first time (`agent heartbeats arriving but frame count has stalled ... failing closed`), but its only lever was `FallbackCover` repeatedly sleeping the display every ~5s with nothing ever actually fixing the underlying dead `SCStream` - confirmed live, a stall that started once just stayed stalled for 4+ minutes until manually recovered with `launchctl kickstart`. No longer a one-off transient hiccup at that point (second occurrence this session under similar conditions) - `CaptureManager` now tracks when it last actually delivered a frame and proactively rebuilds its own stream if that goes stale (`captureStreamStallGraceSeconds`, 15s - deliberately shorter than the daemon's 30s check, so this resolves a stall before the daemon's more disruptive response ever needs to fire). **Closed out the remaining Phase 2 verification loose ends**: the escalation-counter path (5-kills/60s) was re-isolated cleanly on the real Mac - 5 real kills, `terminated(signal: 9)` each time, `escalation threshold reached - locking screen` on the 5th, with no `"heartbeat overdue"` line anywhere in the sequence, confirming `EscalationManager`'s own counter fires independently of the heartbeat-grace path. That same isolation test exposed one more real bug: the escalation lock didn't actually persist - `EscalationManager.onEscalate` called `fallbackCover.show()` directly, bypassing `blackoutTimer` entirely, so `HeartbeatMonitor`'s ordinary auto-clear check (which only checks `!blackoutTimer.isActive`) cleared the "lock" on the very next healthy heartbeat, ~10-15s later - an escalation lock behaving no differently from an ordinary fail-closed cover, defeating the point of a harder-to-shrug-off signal for repeated tampering. Fixed with a dedicated `escalationLockActive` flag, only cleared by the same `AdminRelease`-gated path that already protects `blackoutTimer` - **confirmed working live**: with `escalationKillCount` temporarily lowered to 2 for a deterministic test, three consecutive healthy heartbeats after the lock triggered each logged `"heartbeat looks healthy but escalation lock is active - staying locked until admin release"` instead of silently clearing. Reverted back to 5 immediately after. The boot-race fix (`agentStartupGraceSeconds`) also got a real retest for free, via a genuine (unexplained, but confirmed not caused by anything in this codebase - no kernel panic, no pending software update, and neither target has any code path capable of triggering a system restart, only `pmset displaysleepnow`) cold reboot mid-session: no premature `"failing closed"` line anywhere in the daemon log after that boot. All identified Phase 2 gaps are now closed. **A multi-hour-sleep capture outage surfaced a real self-heal gap, now fixed, plus the deploy pipeline's first genuine end-to-end run surfaced four more real bugs along the way**: after the laptop sat mostly asleep overnight, capture never recovered even after a real, active wake - `agent.log`/`daemon.log`/`pmset -g log` timestamps together reconstructed the cause: `CaptureManager.rebuildAllStreams()` ran during one of macOS's periodic silent maintenance wakes (display still off), found zero displays, but still reset its health-check clock to "now" as if it had succeeded - and `checkStreamHealth()`'s `!streams.isEmpty` guard then permanently stopped evaluating staleness from that point on, since "zero streams" looked identical to "never started." Fixed: `rebuildAllStreams()` no longer marks itself healthy on a zero-display rebuild, and the guard now uses an explicit `hasStartedOnce` flag instead of `streams.isEmpty`, so a zero-display outcome is correctly retried every 15s like any other real stall rather than silently giving up after the first one. Recovering this live required a full rebuild-and-redeploy cycle, and `build-pkg.sh` had apparently never been run start-to-finish successfully before - it surfaced, in order: (1) the same stale `SWIFT_OBJC_BRIDGING_HEADER` issue already fixed once on the daemon target, this time on `ContentGuardRelease` (removed); (2) `ContentGuardRelease`'s `PBXSourcesBuildPhase` was completely empty - `AdminRelease.swift` compiled fine via the target's `fileSystemSynchronizedGroups` entry, but `Config.swift`/`Constants.swift`/`IPCProtocol.swift` live in `Shared/`, outside that synced folder, and were never wired in (added, matching how Agent/Daemon already do it); (3) `build-pkg.sh`'s `BUILD_DIR` was a bare relative `"build/Release"` with nothing telling `xcodebuild` to actually put products there - it was building into DerivedData the whole time, so every step after it (`codesign`, `pkgbuild`) had always been reading from a directory that didn't exist (fixed with an explicit `CONFIGURATION_BUILD_DIR` override, `BUILD_DIR` now absolute); (4) `distribution.xml` had a comment literally spelling out `--package-path`, which is invalid XML - a double hyphen anywhere inside a comment body, not just at its delimiters - and broke `productbuild`'s own parser (reworded, verified this time with a real XML parse, not just eyeballing it). **Once all four were fixed, `build-pkg.sh` ran clean end-to-end for what looks like the first time ever** - build, sign, verify, package, `productbuild` all succeeded - but `sudo installer -pkg ContentGuard-1.0.0.pkg` then refused with "This package requires Rosetta 2 to be installed," even though every actual Mach-O in the payload is confirmed native arm64 (`file` on every binary in `Installer/pkg-root`) and grepping every component's `PackageInfo` plus the combined `Distribution.xml` for any architecture restriction turned up nothing at all. Isolated by installing the three component packages individually instead of through `productbuild`'s combined product archive - confirmed working with no Rosetta involved, so the check is specific to how this macOS version's `installer` processes a product archive, not anything this build actually needs; `build-pkg.sh`'s final instructions now recommend the three individual installs instead. **Confirmed working end-to-end on the real Mac**: fresh daemon and agent pids came up clean, heartbeats and frame counts climbing normally. The self-heal fix itself got a real, unplanned retest for free shortly after: a genuine sleep-then-wake produced exactly the zero-display sequence it was written for (`rebuildAllStreams found 0 displays (likely a silent maintenance wake) - not marking healthy, will retry` twice, then a successful rebuild), and capture was back to real detections within about 90 seconds - before this fix, the equivalent stall never recovered on its own at all. **That same session surfaced a second real, confirmed miss, now fixed**: genuinely explicit Reddit images (inline post-sized, not thumbnails) weren't triggering a block. Temporarily reinstated the per-frame `[debug]` logging (removed once already for a real battery cost - see above) to get real numbers, and it confirmed the cause directly: `skinPixelRatio()` averages skin coverage across the *whole* captured frame, so an explicit image sitting in a page with a sidebar, header, and whitespace around it gets diluted well below the 0.15 threshold by however much of the screen isn't that image - live data showed 0.005-0.117 for the exact frames in question, and zero `classify()` calls anywhere in that window. A single global average genuinely can't tell "explicit image occupying a third of the screen" apart from "normal desktop skin-tone noise" - this session's own data put both in the same ~0.05-0.12 band, which is exactly the tradeoff last night's threshold raise (0.05->0.15, a real necessary battery fix) made worse as a direct side effect. Fixed with a second, independent prefilter signal: `skinAnalysis()` now also computes the highest skin ratio in any single cell of an 8x8 grid over the same thumbnail (`skinRatioPrefilterBlockThreshold`, 0.35) alongside the existing whole-frame average, and the prefilter passes on *either* clearing its threshold - catches a concentrated blob (a real image, undiluted by surrounding page chrome) even when the whole-frame average alone would miss it, without lowering the whole-frame threshold back down and reopening the battery problem it was raised to fix. **Confirmed against real data in the same session**: the two real detections this surfaced scored `maxBlockSkinRatio` 0.7 and 0.9 (both well above 0.35, both correctly reached the classifier, both scored real 0.76+/0.79+ confidence and force-terminated Chrome), while ordinary browsing in the same session topped out around 0.2 - real margin on both sides, same shape of evidence 0.15 itself was confirmed against. Debug logging removed again afterward, same reasoning as before (a real, measured cost if left running unconditionally, not something to keep "just in case"). |
| 3 | Santa app execution control | **done** - **deliberately MONITOR/blocklist mode, not the original spec's LOCKDOWN** (see `profiles/README.md`'s note on `forceAdminPasswordForAppInstallation`) - standard users install freely, the Phase 2 content blocker is the real backstop against NSFW content regardless of app, and Santa's job narrows to denylisting specific known-bad tools (Tor Browser, etc.) rather than gatekeeping everything. `profiles/system-extension.mobileconfig` filled in with Santa's real, public Team ID/bundle IDs (confirmed via northpole.dev, not guessed). `profiles/santa-config.mobileconfig` written - `ClientMode=1` (MONITOR), StaticRules scaffolded with two placeholders deliberately left unfilled rather than guessed (ContentGuard's own signing cert SHA-256, Tor Browser's Team ID - both need real values pulled on the Mac, not available offline). Full install/verification walkthrough at `mac/docs/PHASE_3_SANTA_SETUP.md`: push `system-extension.mobileconfig` via Fleet first (so the System Extension activates silently, no interactive approval prompt - same reasoning as Phase 2's PPPC profile), install Santa via Fleet's Software library (Fleet Premium already purchased), pull the two real values, fill in and push `santa-config.mobileconfig`, then verify Tor Browser actually gets blocked while ContentGuardAgent/ContentGuardDaemon are unaffected. **Now actually run on the real Mac, and it surfaced two real gaps not anticipated in the original plan.** Step 1's `system-extension.mobileconfig` push showed as applied in Fleet, but proved not to have actually taken effect in time: installing Santa via Fleet's Software library (`santa-2026.7.pkg`, the standard non-lite package) triggered a real interactive "Santa would like to use a new endpoint security extension" prompt - the exact failure mode Step 1 exists to prevent - confirmed via `systemextensionsctl list` showing `[activated waiting for user]` rather than a silent activation. Worked around for now with a manual System Settings toggle to get Santa functionally active, but this is a genuine open gap, not a confirmed fix - a future reinstall or a fresh Mac could hit the same prompt with nobody around to click it, and the actual root cause of why the profile didn't take effect in time hasn't been isolated yet (found in the same investigation: `profiles/pppc.mobileconfig` - a completely separate profile last touched during Phase 1/2 - was independently discovered failing in Fleet's OS settings page with a generic "Authorization or Allowed has an invalid value" error. Chased through two wrong hypotheses before finding the real cause: first suspected stale/pre-re-sign content (ruled out - `CodeRequirement` confirmed to still exactly match the real installed binary via `codesign -d -r-`), then suspected MDM UUID-caching of a prior failure (ruled out - a genuinely fresh `PayloadUUID` failed identically). Real cause, confirmed via web search: macOS 26 (Tahoe, what this Mac runs) now restricts `ScreenCapture` PPPC entries to `Authorization: Deny` or `AllowStandardUserToSetSystemService` only - `Allow` is rejected outright, a real platform restriction that didn't exist or wasn't enforced when this profile was first written and confirmed working during Phase 2. Both alternatives Apple now permits defeat the point (`Deny` obviously; `AllowStandardUserToSetSystemService` reintroduces the live-interactive-click dependency this profile specifically existed to eliminate for a headless launchd agent) - so the `ScreenCapture` entry was removed from the profile entirely rather than switched to either. **Considered, accepted tradeoff, not silently absorbed**: this Mac's existing Screen Recording grant (already in its TCC database from earlier testing) is untouched and keeps working - confirmed via this project's own real reboot/sleep/restart testing that it doesn't reset on its own - but a future fresh install, TCC reset, or migration to a different Mac will need one manual interactive click before capture works, no longer restorable via MDM alone; until that click happens the agent fails closed (a locked screen requiring setup) rather than silently running uncovered. `Accessibility`'s entry is unaffected by this restriction and stays in the profile. PayloadUUID/PayloadVersion bumped again (v3) for the content change; not yet confirmed Verified in Fleet. **Second, real gap found once Santa was active**: `santactl status` failed outright ("An error occurred communicating with the Santa daemon") - northpole.dev's own troubleshooting docs confirmed `com.northpolesec.santa.daemon` needs Full Disk Access (a separate TCC grant, `SystemPolicyAllFiles`) that nothing in the original Phase 3 plan accounted for. New `profiles/santa-tcc.mobileconfig` written to cover it, content copied verbatim from northpole.dev's own reference TCC profile (`northpole.dev/deployment/profile-tcc/`) rather than reconstructed - two automated fetch attempts of that page gave inconsistent, partially-truncated answers for the exact `CodeRequirement`/`Authorization`-vs-`Allowed` details, not trustworthy for something this exact-value-sensitive, so the user copied the real page content directly from their own browser instead. Grants Full Disk Access to all three of Santa's components (`daemon`, `netd`, and a previously-unknown `bundleservice` helper, all present in northpole.dev's own reference profile) rather than guessing which are safe to narrow out, same reasoning as `system-extension.mobileconfig`'s `AllowedSystemExtensions` list. **All three profiles pushed**: `santa-tcc.mobileconfig` fixed `santactl status`'s daemon-communication error (`Mode: Monitor` confirmed once applied), and `pppc.mobileconfig`'s `ScreenCapture`-removal fix cleared Fleet's persistent "Failed" status entirely - all three (PPPC, Santa Configuration, Santa Full Disk Access) moved from Failed/stuck to Verifying. Functional enforcement confirmed live on the Mac regardless of Fleet's own dashboard state settling (see below) - the real-world behavior is what actually matters and it's confirmed working. **Step 4's two real placeholder values filled in and confirmed on the real Mac, not guessed**: ContentGuard's own leaf signing cert SHA-256 (`ef2d4924...17bc31f`, from `santactl fileinfo`'s Signing Chain output - cross-checked against `pppc.mobileconfig`'s CodeRequirement hash via a matching SHA-1, confirming it's the same cert) and Tor Browser's real Team ID (`MADPSAYN6T`, from `codesign -dv` after installing Tor Browser specifically to read its signature). **Confirmed working end-to-end on the real Mac, StaticRules and all**: launching Tor Browser was genuinely blocked by Santa on the first real attempt. `santactl rule --check --path <bundle>` initially, confusingly, reported "No matching rule exists" for both rules despite the real block having just happened - not a bug in the rules, a missing-flag issue in the check command itself: `--check --path` defaults to checking for a `BINARY` (SHA-256) rule per `santactl`'s own `--help` text, and our two rules are a `TEAMID` rule and a `CERTIFICATE` rule, not binary rules - adding `--teamid`/`--certificate` and pointing at the real Mach-O inside `Contents/MacOS/` (not the `.app` bundle directory, which has no code signature of its own to check) resolved it cleanly: `Blocked (TeamID, Static)` / `Allowed (Certificate, Static)`. `PHASE_3_SANTA_SETUP.md`'s Step 5 corrected with the right command. All identified Phase 3 gaps are now closed - the one open, accepted (not hidden) risk is `pppc.mobileconfig`'s `ScreenCapture` removal itself: a future fresh install or TCC reset needs one manual interactive click to re-grant Screen Recording, no longer restorable via MDM alone on macOS 26, documented in that file's own comment and this row above. |
| 4 | Cloudflare Worker (ratchet, Santa sync, profile generation) + web dashboard | not started - **explicit scope decision**: the dashboard is meant to be a single control panel, not just a rule-sync frontend - both Santa app block/approve (via the sync server, replacing today's hand-edit-`santa-config.mobileconfig`-and-repush-through-Fleet process) and `.pkg` software deployment (via calls to Fleet's own REST API - the same one its web UI uses, not yet wrapped by anything here) should be reachable from the one dashboard, so day-to-day management doesn't mean jumping between Fleet's UI and Santa's CLI/StaticRules edits. Both pieces are technically independent (Fleet's API works standalone; a Santa sync server is a separate service) but the Worker/dashboard should present them as one surface. **Three more scope decisions made ahead of building it, same reasoning as Phase 3's MONITOR-vs-LOCKDOWN and StaticRules-vs-sync calls**: (1) **Ratchet mechanics** - tightening a restriction (blocking an app, adding a policy) applies immediately; loosening one (un-blocking an app, removing a policy) requires re-entering a password at the moment of that specific action *and* only takes effect after a 24-hour delay - both together, not either alone. The point is that even an already-open dashboard session can't instantly undo a restriction on impulse - matches why this project exists in the first place. (2) **Dashboard auth** - sits behind the same Cloudflare Zero Trust instance already locked down in Phase 1 (the WARP switch lock - see that phase's row), rather than a separate login system. That Zero Trust access is itself gated by a password deliberately not known to the day-to-day user, so reusing it for the dashboard gets a real, already-established barrier for free instead of building a second, weaker one. (3) **Scope stays Santa + Fleet only** - Phase 2's detection/incident history stays local-only on the Mac, not surfaced here (would need a new mechanism to ship agent/daemon logs off-device, a real design problem on its own and out of scope for now). **Forward-compatibility note, not current scope**: the Worker/dashboard's data model (devices, policies, owners) should be designed generic enough that a hypothetical future ContentGuard-for-Android sibling project, and a second device under a different owner, could be added later without a fundamental rearchitecture - e.g. not hard-coding assumptions that there's exactly one Mac and one user. No Android-specific code or design work happens in this project; this is purely about not painting the data model into a corner. |
| 5 | Lockdown: demote to standard, seal admin credentials in the vault | not started |

Do not skip ahead. Phase 2's PPPC profile needs Phase 1's Fleet instance to
push it to; Phase 5's lockdown needs every earlier phase's verification
checklist to have actually passed, or you lock yourself out of a system
that doesn't yet do what it's supposed to.

## Why Phase 0 has no code

Phase 0 is entirely manual, hands-on-the-hardware steps - creating
accounts, toggling FileVault in System Settings, installing Xcode from the
App Store, pairing WARP to a Zero Trust org. None of it is scriptable from
here: this repo is being worked on from a sandboxed cloud environment with
no access to the physical Mac, its Apple ID, or the Cloudflare account it
enrolls into. `docs/PHASE_0_SETUP.md` is the runbook to follow by hand on
the Mac itself; `scripts/verify-phase0.sh` is a script to *run on that Mac*
afterward (copy it over, or `curl`/paste it) to check the parts of Phase 0
that are actually machine-verifiable before starting Phase 1.

Phases 1-5 do produce real code and configuration (Fleet deployment
config, `.mobileconfig` profiles, the `ContentGuardAgent`/`ContentGuardDaemon`
Swift sources, the Cloudflare Worker, the dashboard) - those land in this
directory, `profiles/`, and sibling `worker/`/`web/` directories as each
phase starts, once Phase 0 is verified complete on the actual hardware.
