<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>PayloadDisplayName</key>
    <string>ContentGuard Chrome Policy</string>
    <key>PayloadIdentifier</key>
    <string>com.contentguard.chrome</string>
    <key>PayloadType</key>
    <string>Configuration</string>
    <key>PayloadUUID</key>
    <string>6ECA9AC0-F965-4483-8362-5B12E50392E8</string>
    <key>PayloadVersion</key>
    <integer>6</integer>
    <key>PayloadScope</key>
    <string>System</string>
    <key>PayloadRemovalDisallowed</key>
    <true/>
    <key>PayloadContent</key>
    <array>
        <dict>
            <key>PayloadType</key>
            <string>com.apple.ManagedClient.preferences</string>
            <key>PayloadIdentifier</key>
            <string>com.contentguard.chrome.payload</string>
            <key>PayloadUUID</key>
            <string>56450B06-03B6-4F67-8CF6-95716E896A8F</string>
            <key>PayloadVersion</key>
            <integer>6</integer>
            <key>PayloadDisplayName</key>
            <string>ContentGuard Chrome Policy Payload</string>
            <key>PayloadContent</key>
            <dict>
                <key>com.google.Chrome</key>
                <dict>
                    <key>Forced</key>
                    <array>
                        <dict>
                            <key>mcx_preference_settings</key>
                            <dict>
                                <!--
                                  ExtensionInstallForcelist: force-installs
                                  each listed extension and locks it -
                                  greyed-out remove/disable controls in
                                  chrome://extensions, auto-reinstalled if
                                  somehow removed (confirmed against Chrome
                                  Enterprise's own policy docs, not
                                  guessed). This is genuinely different
                                  from ExtensionInstallBlocklist above
                                  (which controls what CAN be installed,
                                  removed entirely per 2026-08-24's
                                  loosening) - this one is about what, once
                                  installed, CANNOT be taken back off.

                                  REAL, FUNCTIONAL as of 2026-08-25 -
                                  self-hosted per the user's explicit
                                  choice (see worker/src/extensionUpdate.ts's
                                  own doc comment: this project's own
                                  Cloudflare infra, not the Chrome Web
                                  Store). Extension ID and update.xml
                                  location both confirmed end-to-end
                                  before landing here - not just built,
                                  a real HTTPS GET against the live
                                  .crx endpoint returned 200 with a
                                  byte-exact content-length match
                                  against the locally-built file
                                  (93339510 bytes). This is the
                                  self-hosted path from this comment's
                                  own earlier note: a self-hosted update
                                  manifest XML the extension_id's
                                  update_url points to, serving the
                                  signed .crx - worker/src/extensionUpdate.ts
                                  serves both, backed by R2
                                  (wrangler.toml's EXTENSION_ASSETS
                                  binding).

                                  Re-package + re-upload whenever
                                  chrome-extension/ actually changes -
                                  see chrome-extension/build/README.md.
                                  The extension_id below was previously
                                  described as permanent for the life of
                                  build/dist/key.pem - true as far as it
                                  goes, but "permanent" turned out to mean
                                  "until the key needs replacing," not
                                  "never changes" - see the 2026-09-04
                                  re-key note just below for the real
                                  exception this hit.
                                -->
                                <!--
                                  RE-KEYED 2026-09-04 - a genuinely
                                  different extension ID, not another
                                  remove/restore toggle of the same one
                                  (compare the THREE same-ID toggle
                                  cycles this comment used to document:
                                  67ad128/512c319, 2241a2e/9fe700b,
                                  dddf896/ae8ec72 - all real, all worth
                                  keeping in git history, none of them
                                  the actual fix this time). Root cause,
                                  confirmed through real on-device
                                  testing, not guessed: the ORIGINAL id
                                  (pdhcmfmgdicpkanpigjpgenhhbbollpk) got
                                  silently blocked by Chrome's own
                                  built-in Safe Browsing extension
                                  blocklist - a Google-side, client-
                                  enforced mechanism entirely separate
                                  from this policy (confirmed absent from
                                  a real chrome://policy read) and from
                                  anything wrong with the .crx itself
                                  (confirmed correctly served, byte-exact,
                                  right up to the point Chrome refused to
                                  load it - navigating to
                                  chrome-extension://<old-id>/manifest.json
                                  showed Chrome's own native "<id> is
                                  blocked / This page has been blocked by
                                  Chrome" page, while the identical test
                                  against a known-good, currently-
                                  installed extension's ID loaded its
                                  manifest.json fine). Worked through
                                  0.2.0/0.3.1/0.4.0's real installs, then
                                  stopped on the same id for 0.4.1 with no
                                  code change of its own - most likely
                                  explanation: this extension's real
                                  runtime behavior (silently capturing the
                                  screen every few seconds, auto-closing
                                  tabs on a classifier's verdict) reads
                                  exactly like a spyware/screen-recorder
                                  heuristic, and it took real running time
                                  after the 0.4.0 install for Google's own
                                  telemetry to flag the id - not something
                                  a version bump or re-upload could ever
                                  have fixed, since the id itself was what
                                  got blocked.

                                  Re-keyed by generating a fresh key.pem
                                  (see build/README.md) - resets the
                                  clock, not a permanent fix. The same
                                  behavior pattern could get THIS id
                                  flagged again eventually too - a real,
                                  accepted risk of self-hosting a screen-
                                  capturing extension outside the Chrome
                                  Web Store's own review process, not
                                  something this profile edit alone
                                  closes off.
                                -->
                                <key>ExtensionInstallForcelist</key>
                                <array>
                                    <string>ofcbfgalhkhmpknpkcnefgffdhecdjba;https://panel.lukep009.download/extension/update.xml</string>
                                </array>
                                <!--
                                  DeveloperToolsAvailability: back to 2
                                  (Disallowed) as of this version -
                                  re-tightened now that the loosening's
                                  whole reason to exist is gone. Real
                                  history: was 2 since Phase 3/4, loosened
                                  to 1 on 2026-08-25 (explicit user choice,
                                  "Apply immediately") specifically because
                                  DeveloperToolsAvailability=2 also
                                  disables "Developer mode" on
                                  chrome://extensions, which blocked "Load
                                  unpacked" - the only way to test
                                  chrome-extension/ before it was packaged
                                  and force-installed for real.

                                  CONFIRMED LIVE before re-tightening, not
                                  assumed: the extension is genuinely
                                  policy-installed now (ExtensionInstallForcelist
                                  above) - the user removed the manually-
                                  loaded unpacked copy from
                                  chrome://extensions, and it reappeared
                                  on its own, "installed by your
                                  administrator," no Remove/enable-disable
                                  controls. A policy-forced install
                                  doesn't go through the "Load unpacked"
                                  flow at all, so it doesn't need
                                  Developer mode - this loosening's only
                                  reason to exist is gone.
                                -->
                                <key>DeveloperToolsAvailability</key>
                                <integer>2</integer>
                                <key>IncognitoModeAvailability</key>
                                <integer>1</integer>
                                <key>BrowserGuestModeEnabled</key>
                                <false/>
                                <!--
                                  ExtensionInstallBlocklist removed
                                  entirely (was ["*"], blocking all
                                  installs) rather than set to an empty
                                  array - per Chrome's own real policy
                                  semantics (confirmed against Chrome
                                  Enterprise's own policy docs, not
                                  guessed), an unset policy is the actual
                                  "no restriction" state, same meaning as
                                  removing it. Deliberate, informed user
                                  request, not a default/oversight -
                                  bypasses this project's ratchet
                                  mechanism, which doesn't cover MDM
                                  profile edits at all (see
                                  mac/docs/PHASE_4_DASHBOARD_SETUP.md's
                                  "Loosening MDM profile restrictions"
                                  section). Real gap this reopens: this is
                                  the mechanism that would let a
                                  proxy/VPN/content-unblocking extension
                                  in - accepted as a known, named gap.
                                -->
                                <key>DnsOverHttpsMode</key>
                                <string>off</string>
                                <!--
                                  BrowserSignin: 1 (Enable), not 0
                                  (Disable) - confirmed against Chrome
                                  Enterprise's own real policy values, not
                                  guessed: 0 disables sign-in entirely, 1
                                  allows but doesn't force it, 2 forces it.
                                  Same deliberate, informed loosening as
                                  the extension-install change above.
                                -->
                                <key>BrowserSignin</key>
                                <integer>1</integer>
                            </dict>
                        </dict>
                    </array>
                </dict>
            </dict>
        </dict>
    </array>
</dict>
</plist>
