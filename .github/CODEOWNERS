# Requires review from @yl1nbjaww6741 (the real GitHub account - confirmed
# via list_repository_collaborators, `role_name: admin`) before merging any
# change anywhere in this repo. Deliberate self-commitment device: the point
# is that nothing here - gate thresholds, ratchet policy, the Worker's auth
# logic, the MDM/Santa integration, this file itself - can be quietly
# loosened by a single person alone (self-approval doesn't satisfy GitHub's
# required-review check), including via a Claude Code session asked to
# "help" with a weaker version of any of it.
#
# Real, previously-broken state, fixed here: this file used to say
# @yl1nbajww6741 (transposed letters) and only covered four files under
# app/src/main/kotlin/.../detect,service - a real typo that meant it never
# actually named a valid GitHub user, so even with branch protection
# enabled it would have resolved to nobody. Widened to the whole repo and
# corrected to the real username, confirmed against this repo's own
# collaborator list, not guessed.
#
# Only takes effect once main's branch protection has "Require a pull
# request before merging" + "Require review from Code Owners" enabled -
# SETUP.md section 6 covers why that doesn't actually enforce on this
# repo today (private repo, free GitHub plan) and reuses a different,
# already-working mechanism (a required-reviewer Environment) instead for
# every consequential action a push can trigger - see that section, and
# .github/workflows/build.yml's `publish` job, deploy-worker.yml's
# `deploy` job, and apply-d1-migration.yml's `migrate` job, all gated on
# the same `release` environment/reviewer. This file is kept and kept
# accurate anyway, in case the repo ever moves to a paid plan or goes
# public, at which point it starts enforcing automatically with no
# further changes needed - same reasoning the original narrower version
# already gave for keeping it.

* @yl1nbjaww6741
